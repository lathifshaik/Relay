---
"@relay/bridge": minor
---

Make bridged tools understandable, safe to sign in to, resilient and respectful of site owners: names and descriptions from the app's own code (function names, success messages, form headings), risk levels with MCP tool annotations and one-time confirmation codes for destructive/external actions, an editable labels file, `relay-bridge login/logout` with signed-out detection, rate limiting and Retry-After, endpoint-change detection with live tool-list updates, and robots.txt plus `/.well-known/relay.json` opt-out support.
