---
"@relay/bridge": minor
---

Send the agent less: learn each site's navigation/header/footer and leave it out of pages, answer repeat reads with `unchanged` or only the changes (`_full: true` for everything), save the site map for 24 hours (`--refresh`, `--no-cache`, `--cache-dir`), and log every request with how much was read and sent on.
