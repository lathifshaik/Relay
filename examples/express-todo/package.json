{
  "name": "express-todo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no example tests yet' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@relay/core": "workspace:*",
    "@relay/express": "workspace:*",
    "@relay/schema": "workspace:*",
    "express": "^4.19.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@relay/eslint-config": "workspace:*",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "eslint": "^9.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
